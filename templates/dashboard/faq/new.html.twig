{% extends 'dashboard/index.html.twig' %}

{% block dashboardBody %}
    <div class="row section">
        <div class="pb-4">
            <a class="btn btn-dark" href="{{ path('app_faq_index') }}">
                <i class="fa fa-arrow-left"></i>
            </a>
        </div>
        <h1 class="baskerville">Créer une nouvelle question</h1>

        {{ include('dashboard/faq/_form.html.twig') }}
    </div>
{% endblock %}
