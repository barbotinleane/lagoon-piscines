<form method="post" action="{{ path('app_dashboard_delete', {'id': formation_session.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formation_session.id) }}">
    <div class="text-center my-2">
        <button class="btn btn-lg btn-danger">
            <i class="fa fa-trash"></i>
            Supprimer
        </button>
    </div>
</form>
