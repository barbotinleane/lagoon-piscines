<div id="other-informations" class="row py-4">
    <div class="col-12 col-sm-4">
        <h3 class="baskerville size-3">Renseignements utiles</h3>
        {% include 'formation/ask/steps/reusables/list.html.twig' %}
    </div>

    <div class="col-12 col-sm-8 bg-white rounded-corners text-dark p-5 d-flex flex-column justify-content-between">
        <div class="pb-4">
            <div>
                <div class="pb-3">
                    <i class="fas fa-tools form-icon"></i>
                </div>
                {% if flow.getFormData.isStagiaireMultiple == 1 %}
                    <label class="mt-1 fw-bold" id="prerequis_stagiaires">
                        Les stagiaires savent-ils utiliser les outils suivants ?
                    </label>
                {% else %}
                    <label class="mt-1 fw-bold" id="prerequis_autre">
                        Je sais utiliser les outils suivants
                    </label>
                {% endif %}

                <div style="display:none">
                    {{ form_row(form.prerequisites) }}
                </div>

                <div class="ms-3">
                    <div>
                        <label>Visseuse</label><br>
                        <input
                                type="radio"
                                class="btn-check"
                                name="visseuse"
                                id="oui-visseuse"
                                value="oui"
                                autocomplete="off"
                                {% if prerequisites != null %}
                                    {% if prerequisites.Visseuse == "oui" %}
                                        checked
                                    {% endif %}
                                {% endif %}
                        >
                        <label class="btn btn-outline-success" for="oui-visseuse">Oui</label>

                        <input
                                type="radio"
                                class="btn-check"
                                name="visseuse"
                                id="non-visseuse"
                                value="non"
                                autocomplete="off"
                                {% if prerequisites != null %}
                                    {% if prerequisites.visseuse == "non" %}
                                        checked
                                    {% endif %}
                                {% endif %}
                        >
                        <label class="btn btn-outline-danger" for="non-visseuse">Non</label>
                    </div>

                    <div>
                        <label>Perceuse</label><br>
                        <input
                                type="radio"
                                class="btn-check"
                                name="perceuse"
                                id="oui-perceuse"
                                value="oui"
                                autocomplete="off"
                        >
                        <label class="btn btn-outline-success" for="oui-perceuse">Oui</label>

                        <input
                                type="radio"
                                class="btn-check"
                                name="perceuse"
                                id="non-perceuse"
                                value="non"
                                autocomplete="off"
                        >
                        <label class="btn btn-outline-danger" for="non-perceuse">Non</label>
                    </div>

                    <div>
                        <label>Taloche</label><br>
                        <input
                                type="radio"
                                class="btn-check"
                                name="taloche"
                                id="oui-taloche"
                                value="oui"
                                autocomplete="off"
                        >
                        <label class="btn btn-outline-success" for="oui-taloche">Oui</label>

                        <input
                                type="radio"
                                class="btn-check"
                                name="taloche"
                                id="non-taloche"
                                value="non"
                                autocomplete="off"
                        >
                        <label class="btn btn-outline-danger" for="non-taloche">Non</label>
                    </div>

                    <div class="col-12">
                        <label for="commentaires-outils">Commentaires</label><br>
                        <textarea
                                id="commentaires-outils"
                                class="form-control"
                                name="commentaires-outils"
                                rows="4"
                        ></textarea>
                    </div>
                </div>
            </div>

            <div>
                {% if form.mathematics is defined %}
                    <div class="mt-4 form-check" id="mathematics">
                        {{ form_widget(form.mathematics) }}
                        {% if flow.getFormData.isStagiaireMultiple == 1 %}
                            <label class="fw-bold">
                                Les stagiaires ne sont pas allergiques aux mathématiques de base (+, -, x, :...)
                            </label>
                        {% else %}
                            <label class="fw-bold">
                                {{ form_label(form.mathematics) }}
                            </label>
                        {% endif %}

                        <div class="form-error">
                            {{ form_errors(form.mathematics) }}
                        </div>
                    </div>
                {% endif %}
            </div>
        </div>

        <div class="pt-5">
            <div class="pb-3">
                <i class="fas fa-file-alt form-icon"></i>
            </div>

            <div class="row mt-1">
                <div>
                    <div class="fw-bold">
                        {{ form_label(form.knowsUs) }}
                    </div>
                    <div class="ms-3">
                        {% for child in form.knowsUs.children %}
                            <div class="">
                                <div class="">
                                    {{ form_widget(child, {'attr': {'class': 'form-check-input'}}) }}
                                    <label class="form-check-label" for="{{ child.vars.id }}" >{{ child.vars.label }}</label>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                    <div class="form-error">
                        {{ form_errors(form.knowsUs) }}
                    </div>
                </div>

                <div class="col-12 col-md-8" id="autre_cnn_champ" style="display:none;">
                    <label for="autre_connu_champ">Précisez :</label><br>
                    <input
                            type="text"
                            class="form-control"
                            id="autre_connu_champ"
                            name="other[knowsUs]"
                    >
                </div>
            </div>

            <div class="row mt-3">
                <div>
                    <div class="fw-bold">
                        {{ form_label(form.funding) }}
                    </div>
                    <div class="ms-3">
                        {% for child in form.funding.children %}
                            <div class="">
                                <div class="">
                                    {{ form_widget(child, {'attr': {'class': 'form-check-input'}}) }}
                                    <label class="form-check-label" for="{{ child.vars.id }}" >{{ child.vars.label }}</label>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                    <div class="form-error">
                        {{ form_errors(form.funding) }}
                    </div>
                </div>
            </div>

            {% if form.handicap is defined %}
                <div class="mt-3" id="handicap">
                    <div class="fw-bold">Situation de handicap</div>
                    <div class="form-check">
                        {{ form_widget(form.handicap) }}
                        {{ form_label(form.handicap) }}

                        <div class="form-error">
                            {{ form_errors(form.handicap) }}
                        </div>
                    </div>
                </div>
            {% endif %}
        </div>

        {% include 'formation/ask/steps/reusables/buttons.html.twig' %}
    </div>
</div>