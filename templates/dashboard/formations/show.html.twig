{% extends 'base.html.twig' %}

{% block title %}FormationLibelles{% endblock %}

{% block body %}
    <h1>FormationLibelles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation_libelle.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ formation_libelle.libelle }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ formation_libelle.image }}</td>
            </tr>
            <tr>
                <th>Agrement</th>
                <td>{{ formation_libelle.agrement ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ formation_libelle.slug }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ formation_libelle.duration }}</td>
            </tr>
            <tr>
                <th>Cost</th>
                <td>{{ formation_libelle.cost }}</td>
            </tr>
            <tr>
                <th>Route</th>
                <td>{{ formation_libelle.route }}</td>
            </tr>
            <tr>
                <th>ProgramName</th>
                <td>{{ formation_libelle.programName }}</td>
            </tr>
            <tr>
                <th>Presentation</th>
                <td>{{ formation_libelle.presentation }}</td>
            </tr>
            <tr>
                <th>Goals</th>
                <td>{{ formation_libelle.goals }}</td>
            </tr>
            <tr>
                <th>Place</th>
                <td>{{ formation_libelle.place }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_formation_libelles_index') }}">back to list</a>

    <a href="{{ path('app_formation_libelles_edit', {'id': formation_libelle.id}) }}">edit</a>

    {{ include('dashboard/formations/_delete_form.html.twig') }}
{% endblock %}
