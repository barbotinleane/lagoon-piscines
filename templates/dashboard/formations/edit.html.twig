{% extends 'dashboard/index.html.twig' %}

{% block dashboardBody %}
    <div class="row section">
        <div class="pb-4">
            <a class="btn btn-dark" href="{{ path('app_formation_libelles_index') }}">
                <i class="fa fa-arrow-left"></i>
            </a>
        </div>
        <h1 class="inter-black">Modifier une formation</h1>

        {{ include('dashboard/formations/_form.html.twig', {'button_label': 'Valider'}) }}
        {{ include('dashboard/formations/_delete_form.html.twig') }}
    </div>
{% endblock %}
